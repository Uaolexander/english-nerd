import ZeroFirstConditionalLessonClient from "./ZeroFirstConditionalLessonClient";

export const metadata = {
  title: "Zero and First Conditional — B1 Grammar Exercises — English Nerd",
  description:
    "Practice zero and first conditional sentences in English. B1 grammar lesson: zero conditional for facts and habits (if + present, present), first conditional for real future situations (if + present, will). 4 exercises.",
  alternates: { canonical: "/grammar/b1/zero-first-conditional" },
};

export default function ZeroFirstConditionalPage() {
  return <ZeroFirstConditionalLessonClient />;
}
