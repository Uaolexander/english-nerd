import AdvancedDiscourseMarkersLessonClient from "./AdvancedDiscourseMarkersLessonClient";

export const metadata = {
  title: "Advanced Discourse Markers — C1 Grammar — English Nerd",
  description:
    "Learn advanced discourse markers in English: structuring (in the first instance, with regard to), adding (furthermore, what is more), result (consequently, hence, thereby), reformulating (in other words, that is to say). C1 grammar. 4 exercises.",
  alternates: { canonical: "/grammar/c1/advanced-discourse-markers" },
};

export default function AdvancedDiscourseMarkersPage() {
  return <AdvancedDiscourseMarkersLessonClient />;
}
