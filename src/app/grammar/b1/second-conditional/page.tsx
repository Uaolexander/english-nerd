import SecondConditionalLessonClient from "./SecondConditionalLessonClient";

export const metadata = {
  title: "Second Conditional — B1 Grammar Exercises — English Nerd",
  description:
    "Practice the second conditional in English: if + past simple, would + infinitive. B1 grammar lesson with exercises on hypothetical and unreal present/future situations. 4 exercises.",
  alternates: { canonical: "/grammar/b1/second-conditional" },
};

export default function SecondConditionalPage() {
  return <SecondConditionalLessonClient />;
}
